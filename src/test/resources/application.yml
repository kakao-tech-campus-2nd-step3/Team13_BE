spring:
    config:
        import:
        - optional:database.yml
        - optional:logging.yml
        - optional:application-secret.yml

    app:
      version: v1

    data:
      redis:
        port: 6379
        host: localhost

line:
  bot:
    channel-token: "TEST"

openai:
  api-key: "aaaaaa"
  model: "aa"
  chat-completions: "aaa"
  model-tag: "aaa"
